# Datasource configuration
spring.datasource.url=jdbc:mysql://localhost:3306/tetris
spring.datasource.username=jordan
spring.datasource.password=cocobella
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.current_session_context_class=org.springframework.orm.hibernate5.SpringSessionContext
spring.jpa.properties.hibernate.format_sql=true

# Hibernate lazy loading
spring.jpa.open-in-view=false

# Jackson configuration for JSON serialization/deserialization
spring.jackson.serialization.indent_output=true

# Server configuration
server.port=8080
server.error.include-message=always
server.error.include-binding-errors=always

# Security configuration
# spring.security.user.name=admin
# spring.security.user.password=secret

# Spring Actuator endpoints for monitoring and management
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# Session configuration
# server.servlet.session.timeout=30m
# server.servlet.session.persistent=true

# File upload configuration
# spring.servlet.multipart.max-file-size=128KB
# spring.servlet.multipart.max-request-size=128KB

# Logging configuration
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Application-specific properties
# app.game.max-players-per-room=2
# app.game.min-players-to-start=2

# Configure a scheduled task executor if you have scheduled tasks
spring.task.scheduling.pool.size=4

# Mail configuration
# spring.mail.host=smtp.example.com
# spring.mail.port=587
# spring.mail.username=email_user
# spring.mail.password=email_pass
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# CORS configuration
# spring.web.cors.allow-credentials=true
# spring.web.cors.allowed-origins=http://localhost:3000
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.web.cors.allowed-headers=Content-Type,Authorization
# spring.web.cors.max-age=3600

# Caching configuration
# spring.cache.cache-names=players,rooms
# spring.cache.caffeine.spec=maximumSize=500,expireAfterAccess=600s
